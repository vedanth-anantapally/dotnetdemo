apiVersion: v1
kind: Service
metadata:
    name: vedanthanantapallydotnetdemo
spec:
    type: LoadBalancer
    ports:
    - port: 5000 
    selector:
        app: vedanthanantapallydotnetdemo